# Integration tests

integration-tests.kubernetes-namespace-name=integration-test
# Base64 encoded username
integration-tests.docker-hub-username-base64=${DOCKERHUB_USERNAME_BASE64}
# Base64 encoded password
integration-tests.docker-hub-password-base64=${DOCKERHUB_PASSWORD_BASE64}
